import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#7CFF6B",
    },
    secondary: {
      main: "#5B8CFF",
    },
    background: {
      default: "#0b0f14",
      paper: "#111827",
    },
  },
});
